output {
  	# elasticsearch { 
		# 	# hosts => ["elasticsearch:9200"] 
		# 	hosts => ["${ES_HOST}"] 
		# 	index => "${ES_INDEX}"
		# 	manage_template => true
		# 	template => "/usr/share/logstash/templates/aggregating_es.json"
		# 	template_overwrite => true
		# }
		stdout { codec => json }
		pipeline { send_to => coloco }

  # file {
  #   # path => "${OUTPUT_DIR}"
  # }
}




