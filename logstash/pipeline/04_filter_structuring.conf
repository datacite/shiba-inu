filter {

	#### Structuring
  # aggregate {
  #   doi => "%{doi}"
  #   code => "map['investigations'] += event.get('duration')"
  #   map_action => "create"
  # }

  mutate { 
    gsub => ["occured_at","[\\\"]",""]
  }

  date {
    match => [ "occured_at", "YYYY-MM-dd HH:mm:ss.SSSZ", "ISO8601", "YYYY-MM-dd HH:mm:ss"]
    add_tag => [ "dated" ]
  }
  
   
  # metrics {
  #   meter => [ "investigations_%{doi}" ]
  #   add_tag => "metric"
  # }

	## cleaning
	# if [event][server_name] != "datacite" {
  #   drop { }
  # }
}
